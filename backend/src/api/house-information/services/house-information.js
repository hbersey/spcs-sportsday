'use strict';

/**
 * house-information service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::house-information.house-information');
